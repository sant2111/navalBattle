package com.example.navalbattle.model;

/**
 * Contrato mínimo para el estado serializable de una partida guardada.
 * <p>
 * // TODO: implementa compañero de modelo — esta interfaz es un STUB
 * creado solo para permitir que la capa visual compile contra
 * {@code persistence.GameRepository#loadLastGame()}. El equipo de modelo
 * debe definir aquí los métodos reales que necesite la vista para
 * reconstruir el tablero, los barcos y el turno al cargar una partida.
 *
 * @author Jorge Navia
 * @version 1.0
 */
public interface GameSnapshot {
}
